#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include "monty.h"

/**
* monty_nop - main function
* @stack: arguments
* @arg: arguments
* @line_number: arguments
* Return: 0.
*/
void monty_nop(stack_t **stack, char *arg, unsigned int line_number)
{
	arg = arg;
	line_number = line_number;
	stack = stack;
}
